<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Light"></style>

    <style name="NoActionBarAppTheme" parent="android:Theme.Light">
        <item name="windowActionBar">false</item>
    </style>

</resources>
